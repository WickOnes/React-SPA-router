import {Component} from "react";

export default class Main extends Component{
    render(){
const img = require("../icons/Programmer.png")

        return(
            <div>
                <h2 className="main-title">What should I learn from the beginning?</h2>
<img src={img} alt="none"/>
            </div>

        )
    }
}